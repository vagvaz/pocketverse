"""pocketverse: composable unprivileged sandboxes for AI agents.

Layers bubblewrap (mount/pid/net namespaces), overlayfs (copy-on-write
workspace dirs), and a domain-allowlist forward proxy into a single
YAML-configured sandbox.
"""

__version__ = "0.1.0"
